apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-proxy
  labels:
    app: gateway
    tier: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
        tier: frontend
    spec:
      containers:
      - name: proxy
        image: ghcr.io/imtiazwaraich/sentinel-split/sentinel-gateway:latest
        ports:
        - containerPort: 80
          name: http
        envFrom:
        - configMapRef:
            name: gateway-config
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
